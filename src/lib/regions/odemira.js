/**
 * Odemira region data — re-exports from the original wiki-data module.
 * Keeps backward compatibility while enabling multi-region architecture.
 */

export { ODEMIRA as REGION, SECTIONS, EVENTS_CALENDAR, LANDMARKS } from '../wiki-data.js';
