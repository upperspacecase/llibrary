node_modules
dist
.vite
.env.local
.vercel
.env*.local
.gstack/

# Generated by scripts/bake-region-data.mjs at build time. The directory itself
# is not excluded — git will not descend into an ignored directory, which would
# make the fires.json exception below unreachable.
public/data/*/*
!public/data/*/fires.json
